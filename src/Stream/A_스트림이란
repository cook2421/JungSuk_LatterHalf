[배경]
컬렉션,배열 +  for문,Iterator의 문제점
* 코드가 너무 길다.
* 알아보기 어렵다.
* 재사용성이 떨어진다.
* 데이터 소스마다 다른 방식으로 다뤄야한다. (Collection.sort(), Array.sort())


[스트림(stream)이란?]
스트림은 데이터 소스를 추상화하고, 데이터를 다루는데 자주 사용되는 메소드들을 정의해 놓았다.
* 스트림은 데이터 소스를 변경하지 않는다. (read-only)
* 스트림은 일회용이다.
* 스트림은 작업을 내부 반복으로 처리한다.